diff --git a/chrome/browser/first_run/first_run.cc b/chrome/browser/first_run/first_run.cc
index 1234567..abcdefg 100644
--- a/chrome/browser/first_run/first_run.cc
+++ b/chrome/browser/first_run/first_run.cc
@@ -25,6 +25,10 @@
 #include "url/gurl.h"
 #include "url/url_constants.h"
 
+#ifdef LUNETIX_BUILD
+#include "lunetix/common/lunetix_constants.h"
+#endif
+
 namespace first_run {
 
 namespace {
@@ -45,8 +49,13 @@ std::vector<GURL> GetInitialTabs() {
   std::vector<GURL> tabs;
   
   if (ShouldShowWelcomePage()) {
+#ifdef LUNETIX_BUILD
+    tabs.push_back(GURL(lunetix::kLunetixHomepageURL));
+    tabs.push_back(GURL("chrome://welcome/"));
+#else
     tabs.push_back(GURL(chrome::kChromeUIWelcomeURL));
     tabs.push_back(GURL(chrome::kChromeUINewTabURL));
+#endif
   } else {
     tabs.push_back(GURL(chrome::kChromeUINewTabURL));
   }
diff --git a/chrome/browser/ui/startup/startup_browser_creator_impl.cc b/chrome/browser/ui/startup/startup_browser_creator_impl.cc
index 1234567..abcdefg 100644
--- a/chrome/browser/ui/startup/startup_browser_creator_impl.cc
+++ b/chrome/browser/ui/startup/startup_browser_creator_impl.cc
@@ -35,6 +35,10 @@
 #include "url/gurl.h"
 #include "url/url_constants.h"
 
+#ifdef LUNETIX_BUILD
+#include "lunetix/common/lunetix_constants.h"
+#endif
+
 namespace {
 
 // Determines the URLs to be shown on startup based on preferences.
@@ -55,7 +59,11 @@ std::vector<GURL> GetStartupURLs(Profile* profile,
   
   // If no startup URLs are configured, use the default
   if (urls.empty()) {
+#ifdef LUNETIX_BUILD
+    urls.push_back(GURL(lunetix::kLunetixHomepageURL));
+#else
     urls.push_back(GURL(chrome::kChromeUINewTabURL));
+#endif
   }
   
   return urls;